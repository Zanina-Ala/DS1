pipeline {
    agent any

    stages {

        stage('Checkout') {
            steps {
                checkout scm
            }
        }

        stage('Setup') {
            steps {
                bat 'npm install'
            }
        }

        stage('Parallel Build Matrix') {
            parallel {

                stage('Build-node18 (simulated)') {
                    steps {
                        bat 'echo Building with Node 18 (simulated)'
                        bat 'npm run build'
                    }
                }

                stage('Build-node20 (simulated)') {
                    steps {
                        bat 'echo Building with Node 20 (simulated)'
                        bat 'npm run build'
                    }
                }

            }
        }

        stage('Run (Docker)') {
            steps {
                script {
                    bat "docker build -t react-ci-dev:%BUILD_ID% ."
                    bat "docker run -d -p 8081:80 --name react-ci-dev-%BUILD_ID% react-ci-dev:%BUILD_ID%"

                    bat "ping 127.0.0.1 -n 5 >NUL"

                    bat "npm run test:smoke"

                    bat "docker stop react-ci-dev-%BUILD_ID% || echo already stopped"
                    bat "docker rm react-ci-dev-%BUILD_ID% || echo already removed"
                }
            }
        }

        stage('Archive Artifacts') {
            steps {
                archiveArtifacts artifacts: 'dist/**', fingerprint: true
            }
        }

        stage('Cleanup') {
            steps {
                bat "docker image prune -af || echo cleanup failed"
            }
        }
    }

    post {
        success { echo "Dev pipeline success" }
        failure { echo "Dev pipeline failed" }
    }
}
